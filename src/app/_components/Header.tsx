import React from "react";

const Header = () => {
  return (
    <header className="fixed top-0 z-50 block h-[var(--header-h)] w-full min-w-40 bg-[url('/images/header.png')] bg-contain bg-center bg-repeat" />
  );
};

export default Header;

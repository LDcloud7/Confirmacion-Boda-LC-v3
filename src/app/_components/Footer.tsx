import React from "react";

const Footer = () => {
  return (
    <footer className="fixed bottom-0 z-50 block h-[var(--header-h)] w-full min-w-40 rotate-180 bg-[url('/images/header.png')] bg-contain bg-center bg-repeat" />
  );
};

export default Footer;
